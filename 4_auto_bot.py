import time
import os
import sys
import winsound  # خاص بويندوز لإصدار صوت تنبيه
from datetime import datetime

def run_bot():
    print("🤖 تم تشغيل البوت الآلي.. سأفحص السوق كل 15 دقيقة.")
    print("اترك هذه النافذة مفتوحة واذهب لشرب القهوة ☕\n")
    
    while True:
        try:
            # مسح الشاشة لتبقى نظيفة
            os.system('cls' if os.name == 'nt' else 'clear')
            
            print(f"⏰ وقت الفحص: {datetime.now().strftime('%H:%M:%S')}")
            
            # تشغيل ملف التنبؤ واستقبال النتيجة
            # نستخدم os.system لتشغيل الملف الذي صنعناه سابقاً
            os.system('python 3_predict_now.py')
            
            print("\n😴 سأنام الآن لمدة 15 دقيقة...")
            
            # المؤقت: 900 ثانية = 15 دقيقة
            # يمكنك تعديل الرقم لجعله أسرع أو أبطأ
            time.sleep(900) 
            
        except KeyboardInterrupt:
            print("\n🛑 تم إيقاف البوت يدوياً.")
            break
        except Exception as e:
            print(f"⚠️ حدث خطأ: {e}")
            time.sleep(60) # انتظر دقيقة وحاول مجدداً

if __name__ == "__main__":
    run_bot()